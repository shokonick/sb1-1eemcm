---
const menuItems = ['HOME', 'PORTFOLIO', 'PROJECTS', 'LINKS'];
const subMenuItems = ['LANGUAGES', 'ARCHIVES', 'ABOUT'];
---

<nav class="w-full md:w-64 bg-gradient-to-b from-gray-800 to-gray-700 p-5 rounded-lg shadow-lg border-2 border-gray-600">
  <div class="text-center mb-5">
    <div class="w-32 h-32 mx-auto rounded-full overflow-hidden border-4 border-gray-600 shadow-inner bg-gradient-to-b from-gray-700 to-gray-600">
      <img src="https://shokonick.ddns.net/favicon.ico" alt="Profile Picture" width="128" height="128" class="filter hue-rotate-[83deg]" />
    </div>
  </div>
  
  {menuItems.map((item) => (
    <a
      href="#"
      class="block w-full py-2 px-3 mb-3 text-center font-bold rounded-md bg-gradient-to-b from-gray-700 to-gray-600 border border-gray-500 shadow-md hover:from-green-600 hover:to-green-700 hover:text-black transition-all duration-300 transform hover:scale-105"
    >
      {item}
    </a>
  ))}
  
  <div class="bg-gradient-to-b from-gray-700 to-gray-600 p-3 mt-5 rounded-md border border-gray-500 shadow-inner">
    {subMenuItems.map((item) => (
      <a href="#" class="block text-green-500 mb-1 hover:text-green-400 transition-colors duration-300">
        {item}
      </a>
    ))}
  </div>
  
  <div class="text-center text-xs mt-5 bg-gray-800 p-2 rounded-md border border-gray-600 shadow-inner">
    <p>Best viewed in</p>
    <p>800x600 resolution</p>
  </div>
</nav>